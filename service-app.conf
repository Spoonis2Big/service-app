# Service App Configuration
# This file contains configuration settings for the Service App daemon

# Backend Configuration
BACKEND_PORT=3001
NODE_ENV=production

# Frontend Configuration  
FRONTEND_PORT=3000

# Database Configuration
DB_PATH=/home/matt/service-app/server/service_orders.db

# Network Configuration
# The app will be accessible on all network interfaces (0.0.0.0)
# Access URLs:
# - Local: http://localhost:3000
# - Network: http://192.168.0.116:3000 (or your current IP)

# Service Management
# Services are managed by systemd:
# - service-app-backend.service
# - service-app-frontend.service
#
# Use the control script: ./service-app-control.sh
# Available commands: start, stop, restart, status, logs, enable, disable, test